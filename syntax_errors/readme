This program defines a Matrix class, instantiates two Matrix objects, then adds them together.

1. This simple program is littered with syntax errors that will prevent it from compiling; locate and fix each one, ideally without a syntax highlighter ಠ_ಠ
2. Insert a comment directly above each error-containing line and give a very brief explanation of the error
